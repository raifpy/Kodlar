// iddia etmek anlamında
void main(){
  int sayi = 10;
  assert(sayi == 0);
  
  // dart --enable-asserts <file.dart>
  // Güzel bir debugger. Ama gerçek zamanlı olarak değişen veri türlerinde bir işe yaramayabilir
}